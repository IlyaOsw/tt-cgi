import { Box, Typography } from "@mui/material";

import styles from "./TitleCGI.module.scss";

export const TitleCGI = () => {
  return (
    <Box className={styles.container}>
      <Typography variant="h4">CGI suvepraktika provitöö</Typography>
    </Box>
  );
};
